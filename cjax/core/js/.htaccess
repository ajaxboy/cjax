#Some systems might restrict access to remote unrecognized paths. This ensures the files are here are readable.
Options +Indexes
Order deny,allow
Allow from all

<IfModule mod_rewrite.c>
	RewriteEngine On
	#RewriteBase /
#RewriteRule  ^/?(cjax.*js)$	$1 [NC,QSA,L]
</IfModule>
